{
  "scripts": {
    "start": "node build/app.js",
    "build": "rm -rf build/; npx tsc -p tsconfig.json",
    "dev": "yarn build && yarn start"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "nodemailer": "^6.7.3",
    "puppeteer": "^13.5.2",
    "yargs": "^17.4.0"
  },
  "devDependencies": {
    "@types/yargs": "^17.0.10",
    "typescript": "^4.6.3"
  }
}
